# Focus Rings

Focus ring effect tokens — semantic visual emphasis for focused/active interactive elements. Each ring derives its color from an existing semantic color token where Figma binds one.

See the Storybook documentation under `Foundations → Effects → Focus Rings` for the full token reference.
