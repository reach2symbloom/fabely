# Glows

Glow effect tokens — semantic visual emphasis treatments, not elevation. Implemented as drop-shadow geometry (the same mechanism Shadows uses), but representing a distinct kind of intent: interaction/emphasis rather than surface elevation.

See the Storybook documentation under `Foundations → Effects → Glows` for the full token reference.
