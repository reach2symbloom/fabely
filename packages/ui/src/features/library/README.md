# Library

Product-specific chrome; stays in `src/features/library/`.

Shell folder: this branch (`ft-library`) holds the folder and this
README only. Each piece lands on its own `ft-library-*` sub-branch as a
subfolder here, then merges back into `ft-library`, mirroring how
`highlight` and `chapter-nav` grew.

Each piece is its own folder. Compose across them; do not duplicate
primitives into this tree.

## Pieces

| Piece | Folder | Status |
| --- | --- | --- |
| Library List Item | `library-list-item/` | Merged (`ft-library-list-item`) |
| Library Nav | `library-nav/` | In progress (`ft-library-nav`) |
| [Resume Writing Button](./resume-writing-button/README.md) | `resume-writing-button/` | In progress (`ft-library-resume-writing-button`) |
| [Bookshelf Template](./bookshelf-template/README.md) | `bookshelf-template/` | In progress (`ft-library-bookshelf-template`) |
