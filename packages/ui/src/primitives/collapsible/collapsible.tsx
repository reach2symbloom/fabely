/**
 * Fabely Collapsible primitive — thin-pass re-export of the upstream
 * shadcn component (`src/components/ui/collapsible.tsx`).
 *
 * Unstyled placeholder — not yet matched to Figma. No new props or variants.
 * Public API matches the vendor file; import from this primitive, not the
 * vendor path directly.
 */

export * from '@/components/ui/collapsible';
